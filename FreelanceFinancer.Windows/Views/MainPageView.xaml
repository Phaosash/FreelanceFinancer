<UserControl x:Class="FreelanceFinancer.Windows.Views.MainPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FreelanceFinancer.Windows.Views"
             xmlns:vm="clr-namespace:FreelanceFinancer.Windows.ViewModels" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:MainPageViewModel />
    </UserControl.DataContext>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <!--    TODO: Bind this property to make it collapsible     -->
            <ColumnDefinition Width="{Binding SidebarWidth}" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <!--    Left side content (Page Navigation)       -->
        <Grid Grid.Column="0">
            <StackPanel Orientation="Vertical" Margin="5">
                <TextBlock Text="Freelance Financer" />

                <Button Content="Toggle" Command="{Binding ToggleSidebarCommand}" />
            </StackPanel>
        </Grid>
        
        <!--    Main Content Area       -->
        <Grid Grid.Column="1" Margin="5">
            <!--    TODO: Bind this property to allow pages to change   -->
            <local:DashboardPageView />
        </Grid>
    </Grid>
</UserControl>
